# graphify

@../skills/graphify/SKILL.md

Build or update the knowledge graph for the current project. Run `graphify extract . --backend claude` for full semantic extraction, or `graphify update .` for fast AST-only rebuild. Query the graph before searching raw files.
